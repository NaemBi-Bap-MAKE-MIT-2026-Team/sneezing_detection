# =============================================================================
# requirements.txt — Raspberry Pi 4 (64-bit Raspberry OS / aarch64)
# =============================================================================
#
# 권장 사용법:
#   ./setup_rpi4.sh          # 자동 환경 초기화 (Python 3.12 venv 생성 포함)
#
# 또는 수동 설치:
#   pip install -r requirements.txt
#
# ⚠️  Python 버전 주의
#   Python 3.13: imp 모듈 제거로 tensorflow / tflite-runtime 동작 불가
#   Python 3.12: 모든 패키지 정상 동작 (권장)
#   Python 3.10 / 3.11: 동작하나 ai-edge-litert 최신 버전 미지원 가능
#
# LCD(ST7789) 디스플레이 사용 시 추가 설치:
#   pip install st7789
# =============================================================================

# ── TFLite 추론 ───────────────────────────────────────────────────────────────
# tflite-runtime 후속 패키지. Python 3.10~3.13 + aarch64 공식 wheel 제공.
# (tflite-runtime 최종 버전 2.14.0은 Python 3.9~3.11 전용)
ai-edge-litert>=2.1.2

# ── 수치 연산 ─────────────────────────────────────────────────────────────────
numpy>=1.24.0

# ── 오디오 feature 추출 (log-mel spectrogram) ─────────────────────────────────
librosa>=0.10.0

# ── 실시간 마이크 입력 / 스피커 출력 ─────────────────────────────────────────
sounddevice>=0.4.6

# ── 오디오 파일 I/O (librosa 의존성) ─────────────────────────────────────────
soundfile>=0.12.1

# ── 신호 처리 (librosa 의존성) ───────────────────────────────────────────────
scipy>=1.10.0

# ── LCD 이미지 처리 (ST7789 디스플레이) ──────────────────────────────────────
Pillow>=9.0.0
